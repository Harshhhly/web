<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Global Weather</title>
    <link rel="stylesheet" href="style.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">
</head>
<body>
    <div class="container">
        <h1>Global Weather Dashboard 🌦️</h1>
        <p>Select a country to see the current weather in its capital city.</p>
        
        <div class="input-section">
            <select id="country-select">
                <option value="">Loading countries...</option>
            </select>
        </div>

        <div id="weather-result" class="hidden">
            <h2 id="location"></h2>
            <div class="weather-main">
                <img id="weather-icon" src="" alt="Weather icon">
                <p id="temperature"></p>
            </div>
            <p id="description"></p>
            <div class="weather-details">
                <p><strong>Feels Like:</strong> <span id="feels-like"></span></p>
                <p><strong>Humidity:</strong> <span id="humidity"></span></p>
                <p><strong>Wind Speed:</strong> <span id="wind-speed"></span></p>
            </div>
        </div>
        
        <div id="error-message" class="hidden">
            <p>Could not fetch weather data. Please try again later.</p>
        </div>
    </div>

    <script src="script.js"></script>
</body>
</html>
[25/09, 2:25 pm] Tanmay: body {
    font-family: 'Poppins', sans-serif;
    background-color: #f4f7f6;
    color: #333;
    display: flex;
    justify-content: center;
    align-items: center;
    min-height: 100vh;
    margin: 0;
}

.container {
    background-color: #ffffff;
    padding: 30px 40px;
    border-radius: 12px;
    box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1);
    width: 100%;
    max-width: 500px;
    text-align: center;
}

h1 {
    color: #2c3e50;
    margin-bottom: 10px;
}

p {
    color: #7f8c8d;
    margin-bottom: 25px;
}

.input-section {
    margin-bottom: 20px;
}

#country-select {
    width: 100%;
    padding: 12px;
    border: 1px solid #ddd;
    border-radius: 8px;
    font-size: 16px;
    background-color: #f9f9f9;
    cursor: pointer;
    transition: border-color 0.3s;
}

#country-select:focus {
    outline: none;
    border-color: #3498db;
}

#weather-result {
    padding-top: 20px;
    border-top: 1px solid #eee;
}

#location {
    font-size: 24px;
    font-weight: 600;
    color: #2c3e50;
    margin-bottom: 15px;
}

.weather-main {
    display: flex;
    justify-content: center;
    align-items: center;
    margin-bottom: 10px;
}

#temperature {
    font-size: 48px;
    font-weight: 600;
    margin-left: 15px;
}

#description {
    text-transform: capitalize;
    font-size: 18px;
    color: #7f8c8d;
    margin-bottom: 20px;
}

.weather-details {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 10px;
    text-align: left;
    color: #555;
}

.weather-details p {
    margin: 0;
    background: #f9f9f9;
    padding: 10px;
    border-radius: 6px;
}

.hidden {
    display: none;
}
